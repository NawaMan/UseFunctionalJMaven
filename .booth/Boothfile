# syntax=codingbooth/boothfile:1
# Generated by: booth init new --select claude-code+auto-accept/java+kernel+m2+maven+mvn-install/eclipse/idea/gh/git-credential/python/notebook --variant xfce
# Adjust with : booth init adjust --variant xfce --select claude-code+auto-accept/java+kernel+m2+maven+mvn-install/eclipse/idea/gh/git-credential/python/notebook

arg JDK_VENDOR=temurin
arg JDK_VERSION=25
arg MAVEN_VERSION=3.9.12
arg PYTHON_VERSION=3.13.12

setup claude-code
setup gh
setup jdk ${JDK_VERSION} ${JDK_VENDOR}
setup mvn ${MAVEN_VERSION}
setup python ${PYTHON_VERSION}
setup eclipse
setup idea
setup notebook 18888
setup java-code-extension
setup python-code-extension
setup java-nb-kernel
run --mount=type=bind,target=/tmp/ctx if [ -f /tmp/ctx/pom.xml ]; then mkdir -p /opt/mvn-cache && cp /tmp/ctx/pom.xml /opt/mvn-cache/ && cd /opt/mvn-cache && mvn dependency:resolve -B -q; fi
